"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;
var _tags = require("../actions/tags");
function tagReducer() {
  let tagState = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
  let action = arguments.length > 1 ? arguments[1] : undefined;
  const updateState = newState => Object.assign({}, tagState, newState);

  /**
   * Batch update tags from an array of tag IDs
   * @param {String} key Tag action value prop
   */
  const batchChanges = key => action.tagIDs.reduce((result, tagID) => {
    result[tagID] = action[key];
    return result;
  }, {});
  switch (action.type) {
    case _tags.TOGGLE_TAG_ACTIVE:
      {
        return updateState({
          active: Object.assign({}, tagState.active, batchChanges('active'))
        });
      }
    case _tags.TOGGLE_TAG_FILTER:
      {
        return updateState({
          enabled: Object.assign({}, tagState.enabled, batchChanges('enabled'))
        });
      }
    default:
      return tagState;
  }
}
var _default = exports.default = tagReducer;